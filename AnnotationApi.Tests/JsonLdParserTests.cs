using AnnotationApi.Utils;
using JsonLD.Entities;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.IO;

//using JsonLD.Entities;

namespace AnnotationApi.Tests
{
    [TestClass]
    public class JsonLdParserTests 
    {
        //[TestMethod]
        //public void Deserialize_Given_ValidRouteJsonLd_Should_ReturnRoute()
        //{
        //    // Arrange
        //    var parser = new JsonLdParser();
        //    var reader = new StreamReader("./jsonld.json");
        //    var jsonLd = reader.ReadToEnd();

        //    // Act
        //    IEntitySerializer serializer = new EntitySerializer(new StaticContextProvider());
        //    var route = serializer.Deserialize<Object>(jsonLd);

        //    // Assert
        //    //Assert.IsNotNull(route);
        //    //Assert.AreEqual(410, route.Airline.Id);
        //}

        [TestMethod]
        public void StandardTest()
        {
            //// Arrange
            //var reader = new StreamReader("./annotation.json");
            //var jsonLd = reader.ReadToEnd();

            //// Act
            //IEntitySerializer serializer = new EntitySerializer(new StaticContextProvider());
            //var route = serializer.Deserialize<Object>(jsonLd);

            //// Assert
            ////Assert.IsNotNull(route);
            ////Assert.AreEqual(410, route.Airline.Id);
        }
    }
}
